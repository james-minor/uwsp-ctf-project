export enum LogLevel
{
	DEBUG = 'DEBUG',		// Use for info when developing.
	INFO = 'INFO',			// Standard run-time information.
	WARN = 'WARN',			// Important event that could potentially cause a failure.
	ERROR = 'ERROR',		// Important event that WILL or HAS caused a failure.
}